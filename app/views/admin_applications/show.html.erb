<h1>Admin Applications</h1>

<section class="app-info">
<h3>Application information</h3>
<p>Application Status: <%= @application.status %></p>
</section>

<section class="pet-apps">
  <% @application.pets.each do |pet| %>
  <section id="pet-<%= pet.id %>">
  <%= image_tag(pet.image) %><br/>
  <p>Pet Name: <%= pet.name %></p>
  <p>Age: <%= pet.approx_age %></p>
  <p>Sex: <%= pet.sex%></p><br/>
  <% if @application.status != "Approved" && @application.status != "Denied"%>
  <%= button_to "Approve", "/admin/applications/#{@application.id}", method: :patch, params: {status: "Approved"} %>
  <%= button_to "Deny", "/admin/applications/#{@application.id}", method: :patch, params: {status: "Denied"} %>
  <% end %>
  </section>
  <% end %>
